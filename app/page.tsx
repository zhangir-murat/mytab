'use client';

import { useEffect, useRef, useState, type CSSProperties, type ReactNode } from 'react';
import { ArrowLeft, ArrowRight, Check, ChevronRight, CreditCard, GlassWater, LockKeyhole, Minus, Plus, Settings2, Smartphone, Trash2, Utensils, Wine, Beer, Martini, X, CheckCheck, ReceiptText } from 'lucide-react';
import { Sheet, SheetContent, SheetTitle, SheetDescription } from '@/components/ui/sheet';
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
import { RadioGroup, RadioGroupItem } from '@/components/ui/radio-group';

type Item = { id: string; name: string; short: string; description: string; price: number; category: string; popular?: boolean };
type Line = Item & { qty: number; modifier?: string; key: string };
type Status = 'new' | 'making' | 'ready' | 'delivering' | 'delivered';
type Color = { name: string; hex: string; ink: string };
type Order = { id: number; venue: string; items: Line[]; subtotal: number; tax: number; tip: number; total: number; paymentStatus: 'paid'; paymentMethod: string; createdAt: number; status: Status; color: Color; deliveryItems: Line[]; deliveredCount: number; tabId: string };
type Session = { orders: Order[]; cart: Line[]; tabId: string; nextId: number; closed: boolean };
const MENU: Item[] = [
 {id:'margarita',name:'Spicy Margarita',short:'Margarita',description:'Tequila, lime, agave, jalapeño',price:16,category:'Cocktails',popular:true},
 {id:'espresso',name:'Espresso Martini',short:'Espresso Martini',description:'Vodka, espresso, coffee liqueur',price:17,category:'Cocktails',popular:true},
 {id:'old-fashioned',name:'Old Fashioned',short:'Old Fashioned',description:'Bourbon, bitters, demerara, orange',price:16,category:'Cocktails'},
 {id:'negroni',name:'Negroni',short:'Negroni',description:'Gin, Campari, sweet vermouth',price:16,category:'Cocktails'},
 {id:'mule',name:'Moscow Mule',short:'Moscow Mule',description:'Vodka, ginger beer, fresh lime',price:15,category:'Cocktails'},
 {id:'modelo',name:'Modelo',short:'Modelo',description:'Mexican lager · 12 oz',price:8,category:'Beer',popular:true},
 {id:'ipa',name:'IPA',short:'IPA',description:'Hoppy, citrus-forward · 16 oz draft',price:9,category:'Beer'},
 {id:'lager',name:'Lager',short:'Lager',description:'Crisp & refreshing · 16 oz draft',price:8,category:'Beer'},
 {id:'pilsner',name:'Pilsner',short:'Pilsner',description:'Light, clean, golden · 16 oz draft',price:9,category:'Beer'},
 {id:'sauvignon',name:'Sauvignon Blanc',short:'Sauvignon Blanc',description:'Bright citrus, fresh & dry · 6 oz',price:13,category:'Wine'},
 {id:'pinot',name:'Pinot Noir',short:'Pinot Noir',description:'Red berries, soft finish · 6 oz',price:14,category:'Wine'},
 {id:'prosecco',name:'Prosecco',short:'Prosecco',description:'Crisp Italian sparkling · 6 oz',price:13,category:'Wine'},
 {id:'fries',name:'Fries',short:'Fries',description:'Sea salt, house sauce',price:8,category:'Food',popular:true},
 {id:'burger',name:'Burger',short:'Burger',description:'Beef patty, cheddar, pickles, house sauce',price:17,category:'Food'},
 {id:'tenders',name:'Chicken Tenders',short:'Tenders',description:'Crispy chicken, honey mustard',price:14,category:'Food'},
 {id:'pretzel',name:'Pretzel',short:'Pretzel',description:'Warm soft pretzel, whole-grain mustard',price:9,category:'Food'},
];
const COLORS: Color[] = [{name:'Orange',hex:'#ff781f',ink:'#151008'},{name:'Lime',hex:'#c0fb42',ink:'#111707'},{name:'Blue',hex:'#2464f2',ink:'#ffffff'},{name:'Yellow',hex:'#ffe447',ink:'#171504'},{name:'Pink',hex:'#ff57b1',ink:'#240818'},{name:'Cyan',hex:'#43e5ed',ink:'#072022'},{name:'Purple',hex:'#8953e7',ink:'#ffffff'}];
const STATUS: Record<Status,{title:string;copy:string}> = {new:{title:'ORDER RECEIVED',copy:'Your order was sent to the bar.'},making:{title:'MAKING YOUR ORDER',copy:'Your drinks are being prepared.'},ready:{title:'MAKING YOUR ORDER',copy:'All set. Your server will bring it over shortly.'},delivering:{title:'COMING TO YOU',copy:'Keep your phone visible. Your order is on the way.'},delivered:{title:'DELIVERED ✓',copy:'Enjoy.'}};
const STORAGE = 'tab-elsewhere-v1';
const fresh = (): Session => ({orders:[],cart:[],tabId:`tab-${Date.now()}`,nextId:47,closed:false});
const money = (n:number) => `$${(n/100).toFixed(2)}`;
const sum = (lines:Line[]) => lines.reduce((n,i)=>n+i.price*100*i.qty,0);
const count = (lines:Line[]) => lines.reduce((n,i)=>n+i.qty,0);
const makeLine = (item:Item,qty:number,modifier:string): Line => ({...item,qty,modifier:modifier==='Standard'?'':modifier,key:`${item.id}:${modifier}`});
function Button({children,onClick,secondary=false,disabled=false,className='',...rest}: {children:ReactNode;onClick?:()=>void;secondary?:boolean;disabled?:boolean;className?:string;[key:string]:unknown}) {return <button type="button" className={`button ${secondary?'secondary':''} ${className}`} onClick={onClick} disabled={disabled} {...rest}>{children}</button>}
function Quantity({value,onChange,name='Quantity',allowZero=false}:{value:number;onChange:(v:number)=>void;name?:string;allowZero?:boolean}) {return <div className="quantity"><button aria-label={`Decrease ${name}`} onClick={()=>onChange(value-1)} disabled={value<=(allowZero?0:1)}><Minus size={18}/></button><span aria-live="polite">{value}</span><button aria-label={`Increase ${name}`} onClick={()=>onChange(value+1)} disabled={value>=99}><Plus size={18}/></button></div>}
function ItemIcon({category,size=25}:{category:string;size?:number}){const Icon=category==='Cocktails'?Martini:category==='Beer'?Beer:category==='Wine'?Wine:Utensils;return <Icon size={size} strokeWidth={1.4}/>}
function Marker({order,staff=false}:{order:Order;staff?:boolean}) {const single=order.deliveryItems.length===1;return <div className={`marker ${single?'single':'multiple'} ${order.status==='delivering'?'breathing':''} ${order.status==='delivered'?'complete':''} ${staff?'staff-marker':''}`} style={{'--marker':order.color.hex,'--marker-ink':order.color.ink} as CSSProperties}>
 {staff&&<span className="marker-color">{order.color.name}</span>}
 {single?<><span className="marker-name">{staff?order.deliveryItems[0].short:order.deliveryItems[0].name}</span><strong className="marker-qty">×{order.deliveryItems[0].qty}</strong></>:order.deliveryItems.map(i=><div className="marker-row" key={i.key}><strong>{i.short}</strong><b>×{i.qty}</b></div>)}
 </div>}
function AppleLogo(){return <svg aria-hidden="true" width="22" height="26" viewBox="0 0 24 28" fill="currentColor"><path d="M17.1 4.4c.9-1.1 1.5-2.6 1.4-4.1-1.4.1-3 .9-4 2-1 1.1-1.6 2.5-1.5 4 1.6.1 3.1-.8 4.1-1.9ZM20.5 14.9c0-3.2 2.6-4.7 2.7-4.8-1.5-2.2-3.8-2.5-4.6-2.5-2-.2-3.8 1.2-4.8 1.2s-2.5-1.2-4.2-1.1c-2.1 0-4 1.2-5.1 3-2.2 3.8-.6 9.4 1.5 12.5 1 1.5 2.2 3.1 3.8 3.1 1.5-.1 2.1-1 4-1s2.5 1 4.1.9c1.7 0 2.7-1.5 3.7-3 .9-1.1 1.5-2.5 1.9-3.9-.1 0-3-1.2-3-4.4Z"/></svg>}
function BottomSheet({open,title,description,children,onClose,onBack}:{open:boolean;title:string;description?:string;children:ReactNode;onClose:()=>void;onBack?:()=>void}) {return <Sheet open={open} onOpenChange={v=>!v&&onClose()}><SheetContent side="bottom" className="tab-sheet" showCloseButton={false}><div className="sheet-handle"/><header className="sheet-heading">{onBack&&<button className="icon-button" aria-label="Back" onClick={onBack}><ArrowLeft size={20}/></button>}<SheetTitle>{title}</SheetTitle><button className="icon-button close-sheet" aria-label="Close" onClick={onClose}><X size={20}/></button></header><SheetDescription className={description?'sheet-description':'sr-only'}>{description||title}</SheetDescription>{children}</SheetContent></Sheet>}

export default function Home(){
 const [session,setSession]=useState<Session>(fresh);const [loaded,setLoaded]=useState(false);const [mode,setMode]=useState<'customer'|'staff'>('customer');const [view,setView]=useState<'menu'|'live'|'history'|'closed'>('menu');
 const [category,setCategory]=useState('Popular');const [selected,setSelected]=useState<Item|null>(null);const [qty,setQty]=useState(1);const [modifier,setModifier]=useState('Standard');
 const [sheet,setSheet]=useState<'item'|'cart'|'payment'|'confirm'|'card'|'demo'|null>(null);const [payItems,setPayItems]=useState<Line[]>([]);const [paySource,setPaySource]=useState<'fast'|'cart'>('fast');const [tipRate,setTipRate]=useState('20');const [method,setMethod]=useState('Apple Pay');
 const [phase,setPhase]=useState<'processing'|'paid'|'sent'|'putdown'|null>(null);const [activeId,setActiveId]=useState<number|null>(null);const [queue,setQueue]=useState('new');const [deliveryId,setDeliveryId]=useState<number|null>(null);const [toast,setToast]=useState('');const [wake,setWake]=useState(false);const [storageWarning,setStorageWarning]=useState(false);const [now,setNow]=useState(Date.now());const busy=useRef(false);
 useEffect(()=>{try{const raw=localStorage.getItem(STORAGE);if(raw){const s=JSON.parse(raw);if(Array.isArray(s.orders)&&Array.isArray(s.cart)&&s.nextId>=47){setSession(s);const active=s.orders.filter((o:Order)=>o.tabId===s.tabId&&o.status!=='delivered').at(-1);if(active){setActiveId(active.id);setView('live')}else if(s.closed)setView('closed')}}}catch{setStorageWarning(true)}setLoaded(true);const sync=(e:StorageEvent)=>{if(e.key===STORAGE&&e.newValue){try{setSession(JSON.parse(e.newValue))}catch{}}};window.addEventListener('storage',sync);return()=>window.removeEventListener('storage',sync)},[]);
 useEffect(()=>{if(loaded)try{localStorage.setItem(STORAGE,JSON.stringify(session))}catch{setStorageWarning(true)}},[session,loaded]);
 useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),30000);return()=>clearInterval(id)},[]);
 useEffect(()=>{if(toast){const id=setTimeout(()=>setToast(''),2200);return()=>clearTimeout(id)}},[toast]);
 const rounds=session.orders.filter(o=>o.tabId===session.tabId);const activeOrders=rounds.filter(o=>o.status!=='delivered');const active=session.orders.find(o=>o.id===activeId)||rounds.at(-1);const delivery=session.orders.find(o=>o.id===deliveryId);const runningSubtotal=rounds.reduce((n,o)=>n+o.subtotal,0);const runningPaid=rounds.reduce((n,o)=>n+o.total,0);
 const wantsWake=loaded&&mode==='customer'&&view==='live'&&active?.status!=='delivered';
 useEffect(()=>{if(!wantsWake)return;let lock:any;let disposed=false;const acquire=async()=>{try{if(document.visibilityState==='visible'&&'wakeLock' in navigator){const next=await (navigator as any).wakeLock.request('screen');if(disposed){await next.release();return}lock=next;setWake(true);next.addEventListener('release',()=>setWake(false))}}catch{setWake(false)}};void acquire();document.addEventListener('visibilitychange',acquire);return()=>{disposed=true;document.removeEventListener('visibilitychange',acquire);lock?.release();setWake(false)}},[wantsWake]);
 function openItem(item:Item){setSelected(item);setQty(1);setModifier('Standard');setSheet('item')}
 function addToTab(){if(!selected)return;const line=makeLine(selected,qty,modifier);setSession(s=>({...s,closed:false,cart:s.cart.some(i=>i.key===line.key)?s.cart.map(i=>i.key===line.key?{...i,qty:Math.min(99,i.qty+qty)}:i):[...s.cart,line]}));setSheet(null);setToast(`${qty} ${selected.name}${qty>1?'s':''} added to your tab`)}
 function cartQuantity(key:string,value:number){setSession(s=>({...s,cart:s.cart.map(i=>i.key===key?{...i,qty:value}:i).filter(i=>i.qty>0)}))}
 function beginPay(lines:Line[],source:'fast'|'cart'){setPayItems(lines);setPaySource(source);setTipRate('20');setSheet('payment')}
 const subtotal=sum(payItems),tax=Math.round(subtotal*.08875),tip=Math.round(subtotal*Number(tipRate)/100),total=subtotal+tax+tip;
 function dismissPayment(){setSheet(paySource==='fast'?'item':'cart')}
 async function pay(){if(busy.current||!payItems.length)return;busy.current=true;setSheet(null);setPhase('processing');await new Promise(r=>setTimeout(r,500));
 const id=session.nextId;const used=session.orders.filter(o=>o.status!=='delivered').map(o=>o.color.name);const preferred=COLORS.map((_,i)=>COLORS[(id-47+i)%COLORS.length]);const color=preferred.find(c=>!used.includes(c.name))||preferred[0];
 const order:Order={id,venue:'Elsewhere',items:payItems.map(i=>({...i})),subtotal,tax,tip,total,paymentStatus:'paid',paymentMethod:method,createdAt:Date.now(),status:'new',color,deliveryItems:payItems.slice(0,2),deliveredCount:0,tabId:session.tabId};
 setSession(s=>({...s,orders:[...s.orders,order],nextId:Math.max(s.nextId,id+1),cart:paySource==='cart'?[]:s.cart,closed:false}));setActiveId(id);setPhase('paid');await new Promise(r=>setTimeout(r,650));setPhase('sent');await new Promise(r=>setTimeout(r,650));setPhase('putdown');await new Promise(r=>setTimeout(r,1900));setView('live');setPhase(null);busy.current=false;
 }
 function changeStatus(id:number,status:Status){setSession(s=>({...s,orders:s.orders.map(o=>o.id===id?{...o,status}:o)}))}
 function deliver(o:Order){changeStatus(o.id,'delivering');setDeliveryId(o.id)}
 function finishDelivery(o:Order){const deliveredCount=o.deliveredCount+o.deliveryItems.length;const more=deliveredCount<o.items.length;setSession(s=>({...s,orders:s.orders.map(x=>x.id===o.id?{...x,status:more?'ready':'delivered',deliveredCount,deliveryItems:more?x.items.slice(deliveredCount,deliveredCount+2):x.deliveryItems}:x)}));setDeliveryId(null);setQueue(more?'ready':'all');setToast(more?'Batch delivered. The next items are ready.':`Order #${o.id} delivered`)}
 function closeTab(){if(activeOrders.length){setToast('Your order is still on its way. Close your tab after delivery.');return}if(session.cart.length){setToast('Your tab has unplaced items. Remove them or check out first.');setSheet('cart');return}setSession(s=>({...s,closed:true}));setView('closed')}
 function newTab(){setSession(s=>({...s,tabId:`tab-${Date.now()}`,cart:[],closed:false}));setActiveId(null);setView('menu')}
 function switchMode(m:'customer'|'staff'){setMode(m);setSheet(null)}
 const stateRef=useRef(session);stateRef.current=session;
 useEffect(()=>{const mc=(document as any).modelContext;if(!mc?.registerTool)return;const lifecycle=new AbortController();const definitions=[{name:'tab_read_state',title:'Read TAB orders',description:'Read the local TAB menu, cart, and orders. Payments are simulated.',annotations:{readOnlyHint:true,untrustedContentHint:false},inputSchema:{type:'object',properties:{},additionalProperties:false},execute:async()=>({menu:MENU,...stateRef.current})},{name:'tab_set_mode',title:'Switch TAB mode',description:'Navigate between customer and staff views in the local prototype.',annotations:{readOnlyHint:false,untrustedContentHint:false},inputSchema:{type:'object',properties:{mode:{type:'string',enum:['customer','staff']}},required:['mode'],additionalProperties:false},execute:async(args:unknown)=>{const m=(args as {mode?:string})?.mode;if(m!=='staff'&&m!=='customer')throw new Error('Mode must be customer or staff');setMode(m);setSheet(null);await new Promise<void>(resolve=>requestAnimationFrame(()=>resolve()));return {mode:m}}}];definitions.forEach(t=>{try{void Promise.resolve(mc.registerTool(t,{signal:lifecycle.signal})).catch(()=>{})}catch{}});return()=>lifecycle.abort()},[]);

 const sheetTitle=sheet==='item'?selected?.name||'Your drink':sheet==='cart'?'Your tab':sheet==='payment'?'Payment':sheet==='confirm'?method:sheet==='card'?'Card payment':'Demo controls';
 return <div className={`app ${mode==='staff'?'staff-app':''}`}>
 <header className="topbar"><button className="wordmark" aria-label="TAB home" onClick={()=>mode==='customer'?(setView(session.closed?'closed':'menu')):setDeliveryId(null)}>TAB<span className="logo-dot">●</span></button><div className="header-right">{mode==='staff'?<span className="staff-label">ELSEWHERE / STAFF</span>:<span className="venue-mini">ELSEWHERE <span>BK, NY</span></span>}<button className="demo-trigger icon-button" aria-label="Demo controls" onClick={()=>setSheet('demo')}><Settings2 size={18}/></button></div></header>
 {!loaded?<main className="loading-surface"><span className="loader"/>Opening your tab…</main>:mode==='staff'?<main className={`staff-main ${delivery&&delivery.status==='delivering'?'delivery-active':''}`}>
 {delivery&&delivery.status==='delivering'?<div className="delivery-view"><button className="back-link" onClick={()=>setDeliveryId(null)}><ArrowLeft size={18}/> Back to orders</button><div className="delivery-heading"><span className="eyebrow">ORDER #{delivery.id}</span><span className="pill">Delivering</span></div><p className="eyebrow">YOU’RE CARRYING</p><div className="carrying">{delivery.deliveryItems.map(i=><p key={i.key}>{i.name}<strong>×{i.qty}</strong>{i.modifier&&<small>{i.modifier}</small>}</p>)}</div><p className="eyebrow look-for">LOOK FOR {delivery.color.name.toUpperCase()}</p><Marker order={delivery} staff/><p className="find-phone">Find the matching phone.</p><Button onClick={()=>finishDelivery(delivery)}><CheckCheck size={20}/> DELIVERED</Button></div>:<><div className="staff-title"><div><p className="eyebrow">ELSEWHERE · SERVICE</p><h1>Orders<span>.</span></h1></div><div className="on-shift"><span className="status-dot"/>{session.orders.filter(o=>o.status!=='delivered').length} active orders</div></div><Tabs value={queue} onValueChange={setQueue} className="staff-tabs"><TabsList className="queue-tabs">{['new','making','ready','delivering','all'].map(q=><TabsTrigger value={q} key={q}>{q.toUpperCase()}<span>{session.orders.filter(o=>q==='all'||o.status===q).length}</span></TabsTrigger>)}</TabsList>{['new','making','ready','delivering','all'].map(q=><TabsContent value={q} key={q}><div className="order-grid">{session.orders.filter(o=>q==='all'||o.status===q).sort((a,b)=>b.id-a.id).map(o=><article key={o.id} className={`staff-card status-${o.status}`}><div className="staff-card-header"><h2>#{o.id}</h2><span>{Math.max(0,Math.floor((now-o.createdAt)/60000))<1?'Just now':`${Math.max(0,Math.floor((now-o.createdAt)/60000))}m ago`}</span></div><div className="staff-status"><span style={{background:o.color.hex}}/>{o.color.name} <b>·</b> {o.status==='new'?'Received':o.status}</div><div className="staff-items">{(o.status==='ready'||o.status==='delivering'?o.deliveryItems:o.items).map(i=><p key={i.key}><span>{i.name}{i.modifier&&<small>{i.modifier}</small>}</span><strong>×{i.qty}</strong></p>)}</div><div className="staff-total"><span>Total items: {count(o.items)}</span><span>Paid {money(o.total)}</span></div>{o.status==='new'?<Button onClick={()=>{changeStatus(o.id,'making');setQueue('making')}}>START ORDER <ArrowRight size={18}/></Button>:o.status==='making'?<Button onClick={()=>{changeStatus(o.id,'ready');setQueue('ready')}}>MARK READY <Check size={18}/></Button>:o.status==='ready'?<><div className="ready-color" style={{background:o.color.hex,color:o.color.ink}}>LOOK FOR {o.color.name.toUpperCase()}</div><Button onClick={()=>deliver(o)}>DELIVER <ArrowRight size={18}/></Button></>:o.status==='delivering'?<Button onClick={()=>setDeliveryId(o.id)}>CONTINUE DELIVERY <ArrowRight size={18}/></Button>:<div className="delivered-label"><CheckCheck size={18}/> Delivered</div>}</article>)}</div>{!session.orders.some(o=>q==='all'||o.status===q)&&<div className="empty-state"><ReceiptText size={42} strokeWidth={1}/><h2>{q==='new'?'Ready for the next round.':`No ${q==='all'?'':q+' '}orders yet.`}</h2><p>{q==='new'?'Paid customer orders appear here instantly.':'Orders move here as service progresses.'}</p><button className="text-button" onClick={()=>{setMode('customer');setView('menu')}}>Open customer menu <ArrowRight size={16}/></button></div>}</TabsContent>)}</Tabs></>}
 </main>:<main className={`customer-main view-${view}`}>
 {view==='menu'?<><div className="menu-layout"><section className="venue-section"><div className="venue-kicker"><span className="eyebrow">TONIGHT AT</span><span className="venue-index">01 / BROOKLYN</span></div><h1 className="venue-name">ELSE<br className="desktop-br"/>WHERE<span className="venue-period">.</span></h1><div className="venue-bottom"><p>Brooklyn, NY</p><span className="ordering"><span className="status-dot"/> Ordering available</span></div><div className="venue-rule"/><div className="phone-note"><div className="phone-icon"><Smartphone size={23} strokeWidth={1.4}/></div><p>Your next round.<br/><strong>Delivered to your phone.</strong></p><span className="note-arrow">↗</span></div>{rounds.length>0&&<button className="running-link" onClick={()=>setView('history')}><ReceiptText size={18}/> Your Tab <strong>{money(runningPaid)}</strong><ChevronRight size={16}/></button>}</section>
 <section className="menu-section"><div className="menu-heading"><h2>What are you<br/> drinking?</h2><span className="menu-small">GOOD NIGHTS.<br/>LESS WAITING.</span></div><Tabs value={category} onValueChange={setCategory} className="menu-tabs"><TabsList className="category-tabs">{['Popular','Cocktails','Beer','Wine','Food'].map(c=><TabsTrigger value={c} key={c}>{c}</TabsTrigger>)}</TabsList>{['Popular','Cocktails','Beer','Wine','Food'].map(c=><TabsContent value={c} key={c}><div className="menu-section-label"><span>{c==='Popular'?'THE CROWD FAVORITES':c.toUpperCase()}</span><span>{c==='Popular'?'04':String(MENU.filter(i=>i.category===c).length).padStart(2,'0')} PICKS</span></div><div className="menu-list">{MENU.filter(i=>c==='Popular'?i.popular:i.category===c).map((item,i)=><button className="menu-item" key={item.id} onClick={()=>openItem(item)}><div className="menu-icon"><ItemIcon category={item.category}/></div><div className="menu-item-copy"><div className="item-title">{item.name}{c==='Popular'&&i===0&&<span className="favorite-tag">HOUSE FAVORITE</span>}</div><p>{item.description}</p><span className="mobile-price">${item.price}</span></div><span className="item-price">${item.price}</span><span className="add-circle"><Plus size={18}/></span></button>)}</div></TabsContent>)}</Tabs><div className="menu-foot"><LockKeyhole size={13}/><span>Order. Pay. Phone down. We’ll find you.</span></div></section></div>
 {(session.cart.length>0||activeOrders.length>0)&&<div className="bottom-dock">{activeOrders.length>0&&<button className="active-order-bar" onClick={()=>{setActiveId(activeOrders.at(-1)!.id);setView('live')}}><span className="color-dot" style={{background:activeOrders.at(-1)!.color.hex}}/><span>Order #{activeOrders.at(-1)!.id} · {STATUS[activeOrders.at(-1)!.status].title.toLowerCase()}</span><ChevronRight size={18}/></button>}{session.cart.length>0&&<Button onClick={()=>setSheet('cart')}><span>YOUR TAB <span className="cart-count">{count(session.cart)}</span></span><span>{money(sum(session.cart))} <ArrowRight size={18}/></span></Button>}</div>}
 </>:view==='live'&&active?<div className="live-view">{rounds.length>1&&<div className="round-selector">{rounds.map((o,i)=><button key={o.id} className={active.id===o.id?'selected':''} onClick={()=>setActiveId(o.id)}>Round {i+1} <span style={{background:o.color.hex}}/></button>)}</div>}<Marker order={active}/><div className="live-details"><div className="live-meta"><span>ORDER #{active.id}</span><span>{active.color.name.toUpperCase()} <i style={{background:active.color.hex}}/></span></div><div className="status-copy" aria-live="polite"><div className={`status-symbol ${active.status==='delivered'?'is-done':''}`}>{active.status==='delivered'?<Check size={19}/>:active.status==='delivering'?<ArrowRight size={19}/>:<span className="tiny-loader"/>}</div><h1>{STATUS[active.status].title}</h1><p>{STATUS[active.status].copy}</p></div><div className="status-track">{['new','making','delivering','delivered'].map((s,i)=><span key={s} className={i<=({new:0,making:1,ready:1,delivering:2,delivered:3}[active.status])?'filled':''}/>)}</div><div className="live-items">{active.items.map(i=><p key={i.key}><span>{i.name}{i.modifier&&<small>{i.modifier}</small>}</span><strong>×{i.qty}</strong></p>)}</div>{active.items.length>2&&active.status!=='delivered'&&<p className="batch-note">Delivered in small batches. Your marker shows the next items coming to you.</p>}{active.status==='delivered'?<div className="after-delivery"><Button onClick={()=>setView('menu')}>ORDER ANOTHER ROUND <ArrowRight size={18}/></Button><div className="paired-actions"><button onClick={()=>setView('history')}>VIEW TAB</button><button onClick={closeTab}>CLOSE TAB</button></div></div>:<><div className="keep-visible"><Smartphone size={24} strokeWidth={1.4}/><p>Keep your phone face-up.<br/><strong>We’ll find you.</strong></p></div><span className="wake-note">{wake?'Your screen is staying awake.':'Keep this screen open while you wait.'}</span><button className="text-button live-tab-link" onClick={()=>setView('history')}>View tab · {money(runningPaid)} <ChevronRight size={15}/></button></>}</div></div>:view==='history'?<div className="history-view"><button className="back-link" onClick={()=>setView(active?'live':'menu')}><ArrowLeft size={18}/> Back</button><p className="eyebrow">ELSEWHERE · TONIGHT</p><h1>Your tab<span>.</span></h1><p className="muted">Good company. Great rounds.</p>{rounds.length===0&&<div className="empty-state"><GlassWater size={38}/><p>Your first round is waiting.</p></div>}{rounds.map((o,index)=><article className="round-card" key={o.id}><div className="round-header"><h2>Round {index+1}</h2><span className="pill">{o.status==='delivered'?'Delivered':o.status==='new'?'Received':o.status==='delivering'?'Coming to you':o.status==='ready'?'Ready':'Making'}</span></div>{o.items.map(i=><div className="bill-row" key={i.key}><span>{i.name} ×{i.qty}</span><span>{money(i.price*100*i.qty)}</span></div>)}<div className="round-foot"><span>#{o.id} · Paid with {o.paymentMethod}</span><button className="text-button" onClick={()=>{setActiveId(o.id);setView('live')}}>View order <ChevronRight size={14}/></button></div><div className="round-fees">Tax {money(o.tax)} · Tip {money(o.tip)} · Total {money(o.total)}</div></article>)}<div className="tab-totals"><div className="bill-row"><span>Running subtotal</span><strong>{money(runningSubtotal)}</strong></div><div className="bill-row muted"><span>Tax & tips</span><span>{money(runningPaid-runningSubtotal)}</span></div><div className="bill-row grand-total"><span>Total paid</span><strong>{money(runningPaid)}</strong></div></div><Button onClick={()=>setView('menu')}>ORDER ANOTHER ROUND <ArrowRight size={18}/></Button><Button secondary onClick={closeTab}>CLOSE TAB</Button>{activeOrders.length>0&&<p className="close-note">You can close your tab once every round is delivered.</p>}</div>:view==='closed'?<div className="closed-view"><div className="closed-icon"><Check size={38}/></div><p className="eyebrow">ELSEWHERE · BROOKLYN</p><h1>Tab closed.<br/>Night made.</h1><p>Thanks for coming.</p><div className="closed-total"><span>{rounds.length} round{rounds.length!==1?'s':''} · All paid</span><strong>{money(runningPaid)}</strong></div><Button onClick={newTab}>START A NEW TAB <ArrowRight size={18}/></Button></div>:null}
 </main>}
 <footer className="app-footer"><span>TAB</span><span>{mode==='staff'?'SERVICE, IN SYNC.':'LESS WAITING. MORE NIGHT.'}</span><span>ELSEWHERE, BK</span></footer>
 {storageWarning&&<p className="storage-notice">This browser can’t save your tab. Keep this page open during the demo.</p>}
 {toast&&<div className="toast" role="status"><Check size={16}/>{toast}</div>}
 <BottomSheet open={!!sheet} title={sheetTitle} onClose={()=>{if(sheet==='payment')dismissPayment();else if(sheet==='confirm'||sheet==='card')setSheet('payment');else setSheet(null)}} onBack={sheet==='payment'?dismissPayment:sheet==='confirm'||sheet==='card'?()=>setSheet('payment'):undefined}>
 {sheet==='item'&&selected&&<><div className="item-detail-top"><div className="detail-icon"><ItemIcon category={selected.category} size={38}/></div><div><p className="muted">{selected.description}</p><p className="detail-price">${selected.price}</p></div></div>{selected.id==='margarita'&&<div className="modifier"><p className="field-label">MAKE IT YOURS</p><RadioGroup value={modifier} onValueChange={setModifier} className="choice-row">{['Standard','Less spicy','Extra spicy'].map(m=><label className={`choice ${modifier===m?'chosen':''}`} key={m}><RadioGroupItem value={m} className="sr-only"/>{m}</label>)}</RadioGroup></div>}<div className="quantity-row"><span>Quantity</span><Quantity value={qty} onChange={setQty}/></div><Button onClick={()=>beginPay([makeLine(selected,qty,modifier)],'fast')}>ORDER {qty} · {money(selected.price*100*qty)} <ArrowRight size={18}/></Button><Button secondary onClick={addToTab}>ADD TO TAB <Plus size={18}/></Button><p className="sheet-footnote">Straight to checkout, straight to your table.</p></>}
 {sheet==='cart'&&<>{session.cart.length?<><p className="muted cart-intro">One more? Make it a round.</p>{session.cart.map(i=><div className="cart-line" key={i.key}><div className="cart-line-top"><div><h3>{i.name}</h3>{i.modifier&&<p className="muted">{i.modifier}</p>}</div><strong>{money(i.price*100*i.qty)}</strong></div><div className="cart-controls"><Quantity value={i.qty} name={i.name} allowZero onChange={n=>cartQuantity(i.key,n)}/><button className="icon-button" aria-label={`Remove ${i.name}`} onClick={()=>cartQuantity(i.key,0)}><Trash2 size={17}/></button></div></div>)}<div className="bill-row cart-subtotal"><span>Subtotal</span><strong>{money(sum(session.cart))}</strong></div><p className="sheet-footnote">Tax and tip calculated at checkout.</p><Button onClick={()=>beginPay(session.cart,'cart')}>CHECKOUT · {money(sum(session.cart))} <ArrowRight size={18}/></Button></>:<div className="empty-state"><GlassWater size={36}/><h2>A round starts with one.</h2><p>Add something from the menu.</p></div>}<Button secondary onClick={()=>{setSheet(null);setView('menu')}}>BACK TO MENU</Button></>}
 {sheet==='payment'&&<><div className="payment-items">{payItems.map(i=><div className="bill-row" key={i.key}><span>{i.name} <b>×{i.qty}</b>{i.modifier&&<small>{i.modifier}</small>}</span><strong>{money(i.price*100*i.qty)}</strong></div>)}</div><div className="tip-heading"><span className="field-label">A LITTLE LOVE FOR THE TEAM</span><span>Tip</span></div><RadioGroup value={tipRate} onValueChange={setTipRate} className="tip-options">{['0','18','20','25'].map(rate=><label key={rate} className={`tip-option ${tipRate===rate?'chosen':''}`}><RadioGroupItem value={rate} className="sr-only"/><strong>{rate==='0'?'No tip':`${rate}%`}</strong><span>{money(Math.round(subtotal*Number(rate)/100))}</span></label>)}</RadioGroup><div className="payment-totals"><div className="bill-row"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="bill-row"><span>Tax <small>(8.875%)</small></span><span>{money(tax)}</span></div><div className="bill-row"><span>Tip</span><span>{money(tip)}</span></div><div className="bill-row grand-total"><strong>Total</strong><strong>{money(total)}</strong></div></div><Button className="apple-pay" aria-label="Apple Pay" onClick={()=>{setMethod('Apple Pay');setSheet('confirm')}}><AppleLogo/> Pay</Button><div className="other-payments"><Button secondary onClick={()=>{setMethod('Google Pay');setSheet('confirm')}}><b className="google-g">G</b> Google Pay</Button><Button secondary onClick={()=>{setMethod('Card');setSheet('card')}}><CreditCard size={19}/> Card</Button></div><p className="payment-disclaimer"><LockKeyhole size={13}/> Demo payment · You won’t be charged</p></>}
 {sheet==='confirm'&&<div className="wallet-confirm"><div className="wallet-brand">{method==='Apple Pay'?<AppleLogo/>:<b className="google-g">G</b>}<span>{method}</span><span className="demo-pill">DEMO</span></div><div className="wallet-merchant"><span className="wallet-logo">TAB</span><div><h3>Elsewhere</h3><p>TAB · Brooklyn, NY</p></div><Check size={19}/></div><div className="demo-card"><CreditCard size={26}/><div><strong>Demo card</strong><p>•••• 4242</p></div><span>SIMULATED</span></div><div className="confirm-items">{payItems.map(i=><p key={i.key}>{i.name} ×{i.qty}</p>)}</div><div className="wallet-total"><span>Total</span><strong>{money(total)}</strong></div><Button onClick={pay}><LockKeyhole size={18}/> Confirm Payment</Button><p className="payment-disclaimer">Simulation only. No payment will be taken.</p></div>}
 {sheet==='card'&&<div className="card-form"><div className="demo-pill">SIMULATED CARD</div><p className="muted">Demo details are prefilled. No real card information is needed or collected.</p><label>Card number<input readOnly value="4242 4242 4242 4242" aria-label="Card number — simulated"/></label><div className="card-fields"><label>MM / YY<input readOnly value="12 / 30"/></label><label>CVC<input readOnly value="123"/></label><label>ZIP<input readOnly value="11237"/></label></div><Button onClick={pay}>PAY {money(total)} <LockKeyhole size={18}/></Button><p className="payment-disclaimer">Demo payment · You won’t be charged</p></div>}
 {sheet==='demo'&&<div className="demo-controls"><p className="muted">One venue. Two sides of the same night.</p><button className={`mode-option ${mode==='customer'?'chosen':''}`} onClick={()=>switchMode('customer')}><Smartphone size={24}/><span><strong>Customer</strong><small>Order a round. Show your marker.</small></span>{mode==='customer'?<Check size={20}/>:<ChevronRight size={20}/>}</button><button className={`mode-option ${mode==='staff'?'chosen':''}`} onClick={()=>switchMode('staff')}><Martini size={24}/><span><strong>Staff</strong><small>Prepare, find, and deliver.</small></span>{mode==='staff'?<Check size={20}/>:<ChevronRight size={20}/>}</button><p className="sheet-footnote">Orders stay in sync in this browser. All payments are simulated.</p></div>}
 </BottomSheet>
 {phase&&<div className="payment-transition" role="status" aria-live="polite">{phase==='putdown'?<><div className="phone-cue"><Smartphone size={82} strokeWidth={1}/><span style={{background:active?.color.hex||COLORS[0].hex}}/></div><h1>Put your phone down.</h1><p>Leave your phone face-up on your table<br/>and we’ll bring your order to you.</p></>:<><div className="success-ring">{phase==='processing'?<span className="loader"/>:<Check size={42} strokeWidth={2}/>}</div><h1>{phase==='processing'?'One moment…':phase==='paid'?'PAID':'ORDER SENT ✓'}</h1><p>{phase==='processing'?'Confirming your demo payment.':phase==='paid'?'You’re all set.':'The bar has your order.'}</p></>}</div>}
 </div>
}
